#pragma once

#include "Tile.h"

class River : public Tile {
public:
	/*
		Constructor - initiates Tile with received location,
		and terrain level for River.
	*/
	River(sf::Vector2i location);

private:

};