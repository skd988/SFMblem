#pragma once

#include "Tile.h"

class Mountain : public Tile {
public:
	/*
		Constructor - initiates Tile with received location,
		and terrain level for Mountain.
	*/
	Mountain(sf::Vector2i location);

private:

};