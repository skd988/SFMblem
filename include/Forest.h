#pragma once

#include "Tile.h"

class Forest : public Tile {
public:
	/*
		Constructor - initiates Tile with received location,
		and terrain level for Forest.
	*/
	Forest(sf::Vector2i location);

private:

};