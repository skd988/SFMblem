#pragma once

#include "Tile.h"

class Bridge : public Tile {
public:
	/*
		Constructor - initiates Tile with received location, 
		and terrain level for Bridge.
	*/
	Bridge(sf::Vector2i location);

private:

};